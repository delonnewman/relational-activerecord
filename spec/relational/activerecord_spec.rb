RSpec.describe Relational::Activerecord do
  it "has a version number" do
    expect(Relational::Activerecord::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
